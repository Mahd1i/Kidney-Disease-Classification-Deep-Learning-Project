artifacts_root: artifacts

data_ingestion:
  root_dir: artifacts/data_ingestion
  
  # الرابط الأصلي
  source_URL: "https://drive.google.com/file/d/1vlhZ5c7abUKF8xXERIw6m9Te8fW7ohw3/view?usp=sharing"
  
  # نستخدم الآن اسم الملف الذي تم تنزيله بواسطة الكود
  local_data_file: artifacts/data_ingestion/data.zip
  
  # هذا هو المجلد الذي سيتم فك الضغط فيه
  unzip_dir: artifacts/data_ingestion

# ------------------------------------------------------------------
# القسم الجديد: لإخبار المشروع بالمسار الفعلي للبيانات غير المضغوطة
# ------------------------------------------------------------------
data_validation:
  root_dir: artifacts/data_validation
  # المسار الفعلي الذي يحتوي على مجلدات (Cyst, Normal, Stone, Tumor)
  unzip_data_dir: artifacts/data_ingestion/CT-KIDNEY-DATASET-Normal-Cyst-Tumor-Stone 
  ALL_REQUIRED_FILES: 
    - Cyst
    - Normal
    - Stone
    - Tumor
# ------------------------------------------------------------------

prepare_base_model:
  root_dir: artifacts/prepare_base_model
  base_model_path: artifacts/prepare_base_model/base_model.h5
  updated_base_model_path: artifacts/prepare_base_model/base_model_updated.h5


training:
  root_dir: artifacts/training
  trained_model_path: artifacts/training/model.h5